# Restart Your Dev Server

1. Press `Ctrl+C` in your terminal to stop the current server
2. Run: `npm run dev`
3. Open: http://127.0.0.1:3000
4. Look for PWA messages in the console
5. Check for install button in browser address bar!

The service worker will now be active even in development mode.
